# 设计系统不是组件库的同义词

很多团队把“有一套按钮和表单”当成设计系统完成，这通常只解决了视觉重复问题。

## 设计系统的三层

- **语义层**：定义什么是 `Primary Action`、`Danger Action`。
- **约束层**：明确场景边界，不允许随意组合破坏体验。
- **协作层**：设计、前端、产品共享同一套命名和变更流程。

> 组件只是载体，不是目标本身。

当系统只剩样式规范时，复杂业务很快会绕开它；当系统能表达业务语义时，团队才会主动复用它。
